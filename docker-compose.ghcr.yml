services:
  maipsummon:
    image: ghcr.io/arcdent/maipsummon:latest
    ports:
      - "3000:3000"
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-change-me-in-production}
    volumes:
      - ./data:/app/data
    pull_policy: always
    restart: unless-stopped
